{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rewards Schema",
  "type": "object",
  "properties": {
    "rewardId": { "type": "string" },
    "playerId": { "type": "string" },
    "amount": { "type": "number" },
    "date": { "type": "string", "format": "date-time" }
  },
  "required": ["rewardId", "playerId", "amount", "date"]
}
